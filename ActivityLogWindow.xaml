<Window x:Class="CybersecurityChatbotPOE.Views.ActivityLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="📊 Activity Log" Height="480" Width="750"
        WindowStartupLocation="CenterOwner"
        Background="#0D1B2A">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="📊 ACTIVITY LOG" FontSize="22" FontWeight="Bold" 
                   Foreground="#2ECC71" HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="LogItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#1B263B" CornerRadius="8" 
                                BorderBrush="#415A77" BorderThickness="1" 
                                Margin="0,6,0,6" Padding="12,10">
                            <TextBlock Text="{Binding DisplayText}" 
                                       Foreground="#E0E1DD" FontSize="13" 
                                       TextWrapping="Wrap"/>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Button Grid.Row="2" Content="Close" Width="100" Height="35" 
                HorizontalAlignment="Center" Margin="0,15,0,0"
                Background="#415A77" Foreground="White" FontWeight="Bold"
                Cursor="Hand" Click="CloseButton_Click"/>
    </Grid>
</Window>