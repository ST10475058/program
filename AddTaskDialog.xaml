<Window x:Class="CybersecurityChatbotPOE.Views.AddTaskDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="➕ Add New Task" Height="400" Width="450"
        WindowStartupLocation="CenterOwner"
        Background="#0D1B2A">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="➕ ADD NEW TASK" FontSize="20" FontWeight="Bold" 
                   Foreground="#2ECC71" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="1" Text="Task Title:" Foreground="#E0E1DD" FontWeight="Bold" Margin="0,5"/>
        <TextBox x:Name="TitleTextBox" Grid.Row="2" Height="35" FontSize="14" 
                 Margin="0,5,0,15" Background="#1B263B" Foreground="White" 
                 BorderBrush="#415A77" BorderThickness="1" Padding="8,5"/>

        <TextBlock Grid.Row="3" Text="Description:" Foreground="#E0E1DD" FontWeight="Bold" Margin="0,5"/>
        <TextBox x:Name="DescriptionTextBox" Grid.Row="4" Height="80" 
                 TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                 Margin="0,5,0,15" Background="#1B263B" Foreground="White" 
                 BorderBrush="#415A77" BorderThickness="1" Padding="8,5"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="CancelButton" Content="Cancel" Width="80" Height="35" Margin="5"
                    Background="#415A77" Foreground="White" Cursor="Hand" Click="CancelButton_Click"/>
            <Button x:Name="AddButton" Content="Add Task" Width="100" Height="35" Margin="5"
                    Background="#2ECC71" Foreground="White" FontWeight="Bold" Cursor="Hand" Click="AddButton_Click"/>
        </StackPanel>
    </Grid>
</Window>